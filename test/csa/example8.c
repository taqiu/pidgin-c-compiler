/*
 *  correct example
 *
 */

int a, b, c[3][4], *p;
double d, e[3], * x[2][2];
char s[41], *sp;

void foo(int a);
int bar(double e, int w);
double * buz(char *s);


int main() {
	int a;
	int i, j;	

	for (i = 0; i < 20; i = b + c[0][a]) {
		for (j = 0; j > e[3]; j) {
			if ( i < j) {
				*x[0][1] = a + b + c[3][4] + d;
			}
		}
	} 

	while ( *p < 43) {
		while (*p) {
			if (s[3]) {
				foo(a);
			} else {
				a = bar(a, b);
				x[1][1] = buz(s);
			}
		}
	}
}

void foo(int a) {
	int x;
	foo(x);
}

int bar(double e, int w) {
	e = w * 3 + (d + 43543) * 9.09;
	
	return w;
}

double * buz(char *s) {
	int a, b;
	double d;	

	d = a * b + c[2][2] / *p; 
	return &d;
}







