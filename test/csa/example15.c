/*
 *  error example for expression 
 *
 */

int main() {
	int a, b[4][a], *c;
	double i, j[a], k[a+*c];
	void foo();

	for (a;a;a) {
		int baz(double d);
	
		a = a * (b[1][2] - *c) + i;
		*c = b[4][a] + i - (+a);
	}
}
