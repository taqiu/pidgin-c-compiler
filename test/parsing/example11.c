/* test case for block and compoud statements */
int main() {
}

int foo() {
	int asd, *df;
}

int bar() {
	x = i + (h + e) + (f * -i) * !u;
}

int forloop() {
	for (i = 1; d < f; d = d + f) {
	}
	for (i = &d; a == d; a = fc(df)) {
		double d;
		int s, *t;
		break;
	}
	for (asd = *dd; d + d; d = iu[9]) {
		char c;

		u = o + d;
		i = & iu[8];
		continue;
	}
}

int whileloop() {
	while(f == 9) {

	}
	while (fc(d)) {
		double i;
	}
	while (0 == ds) {
		df = sd;
		length(ui);
		
		for (i = 9; i < 100; i = d) {
			oi = dsf + dsf;
		}
		return sd;
	}	
}

int ifelse() {
	if (9 > k) {
		if (i < d) {
			if (fd == d) {
				while (dd == df) {
					if (989798 == iu) {
					} else {
					}
				}
			}
		}
		so = sdf(df) + df;
	}

	simple_stmt = df + df();
		
	if (!oi) {
		if (!iu && !od) {
			double _d989;
		}
	} else {
		if (d) {
			*d = *d + *y;
			i = &d;
		} else {
			fc(iu);
			dd = as(uf);
			o = i + o - id * d;
		}
	} 
}


