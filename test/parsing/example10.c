/* Test case for function call */

double foo (int);
double bar();

int main() {
	j = foo(98);
	bar();
	u = o + y;
	print(" dsf", &f, *f[o][90], &f[80]);
}

double foo (int a) {
	a = 89890;
}

void bar() {
}

