/* test case for expression */

int main() {
	989;
	98.00;
	"sdfasdf";
	dd;
	d[0][99][98][0][87];
	fn_call(d, fn_call());
	(ff + dd);
	(pp);
	2 + 4;
	e * d;
	d - f;
	dd / df;
	df = d + ((((dd + d) / (d - dd))) + df + d) - 89 + (90 * d + df);
	call(8+9+(dfsd+9)*d, (df+90) -909);
}
